# -----------------------------
# General
# -----------------------------

set -g default-terminal "tmux-256color"

set -sg escape-time 0
set -g focus-events on

set -g history-limit 100000

# Faster pane switching
set -s repeat-time 300

# -----------------------------
# Prefix
# -----------------------------

unbind C-b
set -g prefix C-a
bind C-a send-prefix

# -----------------------------
# Splits
# -----------------------------

bind - split-window -v -c "#{pane_current_path}"
bind _ split-window -h -c "#{pane_current_path}"

unbind '"'
unbind %

# -----------------------------
# Vim Navigation
# -----------------------------

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize panes quickly
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# -----------------------------
# Mouse
# -----------------------------

set -g mouse on

# -----------------------------
# Copy Mode (vim style)
# -----------------------------

setw -g mode-keys vi

# Sync tmux's copy buffer with the macOS system clipboard
set -g set-clipboard on

bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-pipe-and-cancel "pbcopy"
bind -T copy-mode-vi MouseDragEnd1Pane send -X copy-pipe-and-cancel "pbcopy"

# -----------------------------
# Easier Reload
# -----------------------------

bind r source-file ~/.tmux.conf \; display "tmux config reloaded"

# -----------------------------
# Pane & Window Indexing
# -----------------------------

set -g base-index 1
setw -g pane-base-index 1

set -g renumber-windows on

# -----------------------------
# Status Bar
# -----------------------------

# Appearance (colors, status-left/right, window formats, pane borders) is owned
# by the catppuccin/tmux plugin loaded below. tpm runs last and overrides any
# manual styling, so only structural prefs live here.
set -g status-position bottom
set -g status-justify left
set -g status-left-length 50
set -g status-right-length 100

# -----------------------------
# Better Window Switching
# -----------------------------

bind Tab last-window

bind -n M-h previous-window
bind -n M-l next-window

# -----------------------------
# Zoom pane
# -----------------------------

bind z resize-pane -Z

# -----------------------------
# Open new windows in cwd
# -----------------------------

bind c new-window -c "#{pane_current_path}"

# -----------------------------
# Performance
# -----------------------------

set -g assume-paste-time 0

# Status bar only shows minutes, so a slower refresh is plenty
set -g status-interval 5

# -----------------------------
# Terminal features
# -----------------------------

# Declare capabilities using terminal-features (tmux 3.2+), which supersedes
# terminal-overrides for RGB, undercurl, and cursor-shape passthrough.
# RGB:       true-color support (replaces the old Tc override)
# usstyle:   colored underlines (error/warn squiggles in nvim diagnostics)
# cstyle:    cursor-shape passthrough (block in normal, beam in insert)
set -as terminal-features '*:RGB:usstyle:cstyle'

#------------------------------
# Plugins
# -----------------------------

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux'

# Appearance — must be set BEFORE the plugin loads (the tpm run line below).
# Note: option is @catppuccin_flavor (US spelling) in catppuccin/tmux v1.0+.
set -g @catppuccin_flavor 'mocha'        # match nvim (catppuccin-mocha), ghostty, bat
set -g @catppuccin_window_status_style 'rounded'
set -g @catppuccin_date_time_text ' %Y-%m-%d %H:%M '

run '~/.tmux/plugins/tpm/tpm'

# Status line composition — must come AFTER the plugin loads above. In v1.0+
# setting the flavor alone renders no status content; left/right are built from
# catppuccin modules (the window list is themed by the plugin automatically).
set -g status-left '#{E:@catppuccin_status_session}'
set -g status-right '#{E:@catppuccin_status_date_time}'
